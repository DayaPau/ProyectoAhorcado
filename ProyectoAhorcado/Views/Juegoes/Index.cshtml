@model IEnumerable<ProyectoAhorcado.Models.Juego>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PalabraOculta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntentosRestantes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Palabra)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PalabraOculta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntentosRestantes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Palabra.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
